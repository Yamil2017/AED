#include "Stack.h"

#include<random>
#include<ctime>
#include<iostream>

/*
int main(){
	srand(time(0));
	Pila nueva(5);
	for(int i=0;i<nueva.TAMANIO;i++)
		push(nueva,1+rand()%15);
	impresion(nueva);
	std::cout<<top(nueva)<<'\n';
	pop(nueva);
	std::cout<<top(nueva)<<'\n';
	pop(nueva);
	impresion(nueva);
	push(nueva,rand()%354);
	std::cout<<top(nueva)<<'\n';
	impresion(nueva);
	vaciar(nueva);
	std::cout<<top(nueva)<<'\n';
	impresion(nueva);

	return 0;
}
*/
