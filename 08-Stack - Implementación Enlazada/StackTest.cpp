/*#include"Stack.h"
#include<random>
#include<ctime>
#include<iostream>

int main(){

	srand(time(0));
	Pila nueva;
	int elementoEliminado;
	for(int i=0;i<35;i++)
		push(nueva,rand()%800);
	print(nueva);
	vaciar(nueva);
	print(nueva);
	for(int i=0;i<5;i++)
			push(nueva,rand()%800);
	print(nueva);
	pop(nueva,elementoEliminado);
	std::cout<<"Elemento "<<elementoEliminado<<" eliminado\n";
	pop(nueva,elementoEliminado);
	std::cout<<"Elemento "<<elementoEliminado<<" eliminado\n";
	print(nueva);

	return 0;
}
*/
