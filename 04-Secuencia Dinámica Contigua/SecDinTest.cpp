/*#include<random>
#include<ctime>
#include<iostream>

//#include"SecDin.h"
#include"SecDinTemplate.h"

int main() {

	srand(time(0)); //Prueba SecDin.h
	Lista nueva(20);
	for (int i = 0; i < tamanio(nueva); i++)
		set(nueva, i, rand()%49);
	print(nueva);

	Lista<std::string, 3> arreglo;
	set(arreglo, "Yamil",0);
	set(arreglo, "es", 1); 
	set(arreglo, "ingeniero.", 2);
	print(arreglo);

	system("pause");
	return 0;
}*/